#include <bits/stdc++.h>
using namespace std;

int main() {
	for (int x = 0 ; x < 431; ++x) {
		if ((43 * x) % 431 == 1)
			cout << "val of x is: " << x << "\n";
	}

	for (int y = 0; y < 2021; ++y) {
		if ((201 * y) % 2011 == 1) {
			cout << "val of y is: " << y << "\n";
		}
	}
	return 0;
}
